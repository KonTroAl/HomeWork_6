class Road:
    def __init__(self, length, width):
        self._length = length
        self._width = width

    def my_func(selfs):
        print(f"Масса асфальта для заданных параметров равна: {selfs._length * selfs._width * 0.025 * 5} т.")


track = Road(5000, 20)
track.my_func()